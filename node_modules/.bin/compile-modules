#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../es6-module-transpiler/bin/compile-modules" "$@"
  ret=$?
else 
  node  "$basedir/../es6-module-transpiler/bin/compile-modules" "$@"
  ret=$?
fi
exit $ret
